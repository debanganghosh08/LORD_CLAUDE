"""LORD core: deterministic, model-agnostic engineering tooling.

The package is standard-library only. It provides the harness half of the
MODEL + LORD equation: workspace boundaries, repository forensics, reuse and
change-surface analysis, impact analysis, and structured reports that any
IDE-hosted model can consume. It never calls a model API.
"""

__version__ = "0.1.0"
