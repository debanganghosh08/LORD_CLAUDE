[project]
name = "ledger-demo"
version = "0.1.0"
description = "Small expense ledger used as the LORD acceptance environment."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
