{
  "doing": "Phase 8A.1 remediation implemented and committed; next is the live Baseline B re-evaluation (Gemini 3.1 Pro High, IDE, fresh conversations, record --series B) for T01, T03, T04, T05, T07, T08 (+T02, T06), then sections 14-15 of the remediation report",
  "done": [
    "context counts as evidence; surfaced files are evidence; task frame with open-question gate and assumption advisory; PreInvocation reminder; containment copy detection; AST bypass signals; measure(only=); verify report block; T04 check; series label; single launcher; docs/skills/contract updated; 199 tests"
  ],
  "remaining": [
    "Run Baseline B live and record with --series B",
    "Fill report sections 14 and 15 (Baseline B results, before/after matrix) and score with the unchanged scorecard",
    "Decide whether the Verification block becomes a scorecard requirement"
  ],
  "discovered": [
    "The brief on a file dropped dependent findings, so a file-level context could not surface its callers; kept now (keep['dependent']=8)",
    "Containment at 0.60 flags nested functions (containment 1.0 by construction) and many small-method pairs; 0.75 plus the enclosing-symbol exclusion leaves only plausible copies",
    "The old baseline contained self-referential text matches (84 in refs-format_amount.json); baseline artefacts are now scoped to demo/"
  ],
  "decided": [
    "Bypass and copy signals are advisories (Level 1), never blocks; the open-question gate is a block because the question is the model's own recorded fact",
    "load_task/load_handoff similarity (0.81) left as is: a shared loader would couple the session store to the versioned memory store"
  ],
  "next": [
    "Reset demo, index and .lord/session before each Baseline B scenario (manual guide section 4)",
    "After Baseline B: compare A vs B per dimension, update the report, commit, push"
  ],
  "verification": "python -m pytest: 199 passed; demo: 37 passed; oracle on unfixed demo: 3 fail (expected)",
  "updated": "2026-09-20",
  "branch": "main",
  "commit": "4cb5eaa"
}
